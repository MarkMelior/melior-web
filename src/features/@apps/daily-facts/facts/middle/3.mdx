🧩 **Lazy loading экономит мегабайты**

Зачем грузить всё приложение сразу, если часть страниц пользователь никогда не откроет?

```jsx hideHeader
const Profile = React.lazy(() => import('./Profile'));

<Suspense fallback={<div>Загрузка...</div>}>
  <Profile />
</Suspense>
```

> Пользователь загружает код только тогда, когда он ему нужен.

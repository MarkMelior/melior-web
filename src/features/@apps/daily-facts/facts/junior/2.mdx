🌈 **CSS-переменные экономят время**

Вместо того чтобы менять один и тот же цвет в 20 местах, можно задать переменную:

```css hideHeader
:root {
  --main-color: #ff5733;
}

button {
  background: var(--main-color);
}
```

> Хочешь новый цвет? Меняешь одну строчку — и всё обновляется.

🎨 **Порядок подключения CSS и JS имеет значение**

Браузер рендерит страницу сверху вниз. Если поставить `<script>` в начале, загрузка замедлится — JS блокирует построение страницы.

✅ Правильно:

```html hideHeader
<head>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <h1>Привет!</h1>
  <script src="app.js"></script>
</body>
```

❌ Неправильно:

```html hideHeader
<head>
  <script src="app.js"></script> <!-- блокирует -->
  <link rel="stylesheet" href="styles.css">
</head>
```

> Сначала CSS, потом JS — так страница отобразится быстрее.

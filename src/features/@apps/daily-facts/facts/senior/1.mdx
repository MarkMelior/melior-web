🪶 **CSS `content-visibility` ускоряет рендер**

Элемент за пределами экрана можно не рендерить, пока он не нужен:

```css hideHeader
.card {
  content-visibility: auto;
}
```

> Это снижает нагрузку на процессор при большом DOM.
